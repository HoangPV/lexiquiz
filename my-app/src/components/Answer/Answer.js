import React, { useState } from 'react';

const Answer = ({text}) => {
    // Answer component logic and state management
    return (
        <li className="answer-item">{text}</li>
    );
};

export default Answer;
